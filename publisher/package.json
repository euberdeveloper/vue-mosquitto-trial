{
  "name": "vue-mosquitto-trial-publisher",
  "version": "1.0.0",
  "description": "",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "start:mqtt": "node dist/main MQTT",
    "start:ws": "node dist/main WS",
    "serve": "npm run build && npm run start:mqtt",
    "serve:mqtt": "npm run build && npm run start:mqtt",
    "serve:ws": "npm run build && npm run start:ws"
  },
  "author": "Eugenio",
  "license": "MIT",
  "devDependencies": {
    "@types/mqtt": "^2.5.0",
    "typescript": "^3.4.3"
  },
  "dependencies": {
    "@types/node": "^11.13.2",
    "mqtt": "^2.18.8"
  }
}
